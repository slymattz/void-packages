Mozilla rustc check does not support crossbuild: let's remove it
Remove calls to unwrap_rustup, they fail if rustup isn't present

Do not try to figure out the rust target as it is broken on musl
instead use what is set in RUST_TARGET

Skip extra checks on cross builds (broken for arm*)

--- a/build/moz.configure/rust.configure	2026-09-22 19:46:08.000000000 +0200
+++ b/build/moz.configure/rust.configure	2026-09-23 20:39:26.166881256 +0200
@@ -89,9 +89,6 @@
     return unwrap
 
 
-rustc = unwrap_rustup(rustc, "rustc")
-cargo = unwrap_rustup(cargo, "cargo")
-
 
 set_config("CARGO", cargo)
 set_config("RUSTC", rustc)
@@ -277,6 +274,9 @@
         data.setdefault(key, []).append(namespace(rust_target=t, target=info))
     return data
 
+@imports('os')
+@imports(_from='mozbuild.util', _import='ensure_unicode')
+@imports(_from='mozbuild.util', _import='system_encoding')
 
 def detect_rustc_target(host_or_target, arm_target, rust_supported_targets):
     # Rust's --target options are similar to, but not exactly the same
@@ -431,12 +431,12 @@
 
         return None
 
-    rustc_target = find_candidate(candidates)
+    rustc_target = os.environ['RUST_TARGET']
 
     if rustc_target is None:
         die("Don't know how to translate {} for rustc".format(host_or_target.alias))
 
-    return rustc_target
+    return ensure_unicode(rustc_target, system_encoding)
 
 
 @imports("os")
@@ -501,6 +501,7 @@
 def rust_host_triple(rustc, host, rustc_host, rust_supported_targets, arm_target):
     rustc_target = detect_rustc_target(host, arm_target, rust_supported_targets)
     if rustc_target != rustc_host:
+        return rustc_host
         if host.alias == rustc_target:
             configure_host = host.alias
         else:
@@ -521,7 +522,6 @@
                 )
             )
         )
-    assert_rust_compile(host, rustc_target, rustc)
     return rustc_target
 
 
@@ -529,7 +529,6 @@
 @checking("for rust target triplet")
 def rust_target_triple(rustc, target, rust_supported_targets, arm_target):
     rustc_target = detect_rustc_target(target, arm_target, rust_supported_targets)
-    assert_rust_compile(target, rustc_target, rustc)
     return rustc_target
 
 


